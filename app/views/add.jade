extend layout

block pageScript
    script.
        pig.init("add");

block content
    section.add-transaction
        form(action="/transaction/add", method="POST")
            div.group
                label(for="amount") Expense
                input(name="amount", id="amount", type="number", placeholder="13.56", autofocus, required)
            div.group
                label(for="date") Date
                input(name="date", id="date", type="date", value="#{today}", placeholder="mm/dd/yyyy", required)
            div.group
                label(for="description") Description
                input(name="description", id="description", type="text", placeholder="optional")
            div.group
                label(for="category") Category
                select(name="category", id="category", size="10", required)
                    - each category, index in categories
                        option(value="#{index}")
                            | #{category}
            div.actions
                input(type="submit", value="Add")
